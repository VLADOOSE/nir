-- Разово при первичной инициализации БД. Без этого планировщик на маленьком реестре (14k строк)
-- игнорирует GIN-индекс и уходит в seq scan → матчинг ~600мс вместо ~60мс (CLAUDE.md §11).
-- pg_trgm-расширение и GIN-индексы создаёт Flyway V1 при старте приложения.
ALTER DATABASE nirdb SET random_page_cost = 1.1;
