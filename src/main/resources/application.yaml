spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nirdb
    username: postgres
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update    # dev; на прод - flyway
  mail:
    host: smtp.example.com
    port: 587
    username: your@email.com
    password: email_password
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true

server:
  port: 8080
